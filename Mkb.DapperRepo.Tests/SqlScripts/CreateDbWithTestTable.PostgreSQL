create database PlaceHolderDbName
with
   ALLOW_CONNECTIONS = true
   CONNECTION LIMIT = 50

{WaitBlock}

create table TableWithAutoIncrementPrimaryKey
(
    Id         serial
        constraint tablewithautoincrementprimarykey_pk
            primary key,
    name       varchar(150),
    SomeNumber int
);

create table TableWithNoAutoGeneratedPrimaryKey
(
    Id         varchar(150)
        constraint TableWithNoAutoGeneratedPrimaryKey_pk
            primary key,
    Name       varchar(150),
    SomeNumber int
);